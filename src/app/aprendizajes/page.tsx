import { Box, Card, CardContent, Chip, Stack, Typography } from '@mui/material';import { aprendizajesConsulta } from '@/lib/operacion';export const dynamic='force-dynamic';export default async function Aprendizajes(){const items=await aprendizajesConsulta();return <Stack spacing={2}><Typography variant="h2">Aprendizajes</Typography><Typography color="text.secondary">Decisiones, resultados y conocimiento validado que puede reutilizarse entre proyectos.</Typography>{items.length===0&&<Card><CardContent><Typography color="text.secondary">Publica minutas para alimentar esta base de conocimiento.</Typography></CardContent></Card>}{items.map(x=><Card key={x.id}><CardContent><Box sx={{display:'flex',gap:1,alignItems:'center'}}><Chip size="small" label={x.tipo}/><Chip size="small" variant="outlined" label={x.polaridad}/><Typography variant="h4">{x.titulo}</Typography></Box><Typography variant="body2" sx={{mt:1}}>{x.detalle}</Typography><Typography variant="caption" color="text.secondary">{x.proyecto} · {x.reunion} · {x.creado_en}</Typography></CardContent></Card>)}</Stack>}
