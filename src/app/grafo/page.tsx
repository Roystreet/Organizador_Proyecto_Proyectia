import { Stack, Typography } from '@mui/material';import GrafoOperacion from '@/components/GrafoOperacion';import { grafoOperacion } from '@/lib/operacion';export const dynamic='force-dynamic';export default async function Grafo(){const grafo=await grafoOperacion(undefined,250);return <Stack spacing={2}><Typography variant="h2">Grafo del portafolio</Typography><Typography color="text.secondary">Proyectos, tareas, reuniones, fases e impactos conectados por sus relaciones reales.</Typography><GrafoOperacion grafo={grafo}/></Stack>}
